{
  "name": "runes-and-remnants",
  "private": true,
  "type": "module",
  "scripts": {
    "check:assets": "node tests/check-assets.mjs",
    "test": "vitest run",
    "test:ui": "playwright test",
    "test:all": "vitest run && playwright test && node tests/check-assets.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "glob": "^11.0.0",
    "handlebars": "^4.7.9",
    "vitest": "^2.0.0"
  }
}
